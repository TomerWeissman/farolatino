"""Pure-Python helpers shared between the FastAPI app (api/) and the
legacy Streamlit app (streamlit_app/). No web-framework imports here.

These three modules used to live under streamlit_app/ but had zero
Streamlit deps, so they moved here for the FastAPI migration.
"""
