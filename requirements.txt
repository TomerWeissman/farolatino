mcp[cli]>=1.0.0
pyyaml>=6.0
httpx>=0.27.0
python-dotenv>=1.0.0
# Per-OS user-data dir (~/Library/Application Support/FaroAI on mac, etc.)
platformdirs>=4.0.0
# Native desktop window (no browser chrome). Mac uses system WebKit via
# PyObjC; Windows uses Edge WebView2 (pre-installed on Win10 1803+).
pywebview>=5.0
pytest>=8.0.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.120.0
google-auth-httplib2>=0.2.0
# Web layer
fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.1
pydantic>=2.5
# V2 LLM provider SDKs — pick one (or more) and the runner auto-detects.
anthropic>=0.40.0
openai>=1.50.0
google-genai>=0.3.0
